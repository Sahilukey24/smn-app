name: smn
description: SMN – Social Media Network. Production-grade Flutter app with Supabase.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  go_router: ^14.2.0
  shared_preferences: ^2.2.3
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  pdf: ^3.10.7
  path_provider: ^2.1.2
  fl_chart: ^0.66.0
  open_filex: ^4.5.0
  file_picker: ^8.0.0
  geolocator: ^13.0.2
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
